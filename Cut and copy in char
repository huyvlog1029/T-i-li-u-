#include <iostream>
#include <math.h>
#include <cstdlib>
#include <fstream> 
#include <string.h>
#include <ctime>
#include <stdio.h>
#include <cmath>

using namespace std; 

int main() { 
	
	char str1[25], str2[25]; 
	strcpy(str1,"clclclcl");
	strcpy(str2,"dmdmdmd");
	strcat(str1,str2)	; 
	puts(str1); 
	
	char ho[55], ten[100],tong[200]; 
	cout<<"ho: "; gets(ho); 
	cout<<"ten: "; gets(ten); 
	
	strcpy(tong,"");
	strcat(tong,ho);
	strcat(tong," ");
	strcat(tong,ten); 
	puts(tong); 
	
	char str3[25], str4[25]; 
	strcpy(str3,"to be "); 
	strcpy(str4,"continue hehe"); 
	strncat(str3,str4,8); 
	puts(str3); 
  return 0; 
  
  
} 
 
